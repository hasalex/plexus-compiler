language: java
jdk:
  - oraclejdk8

cache:
  directories:
  - $HOME/.m2

after_success:
  - curl -T ./pom.xml -H \"X-Bintray-Publish: 1\" -H \"X-Bintray-Override: 1\" -u$BT_USERNAME:$BT_KEY https://api.bintray.com/content/$BT_USERNAME/maven/plexus-compiler/0/pom.xml"
